import React from 'react'
import { lemon } from '../layout'

export default function page() {
  return (
    <main className='flex justify-center items-center h-[100vh]'>
      <div className={`text-[#fb0066] font-bold ${lemon.className}`}>Terms</div>
    </main>
  )
}
