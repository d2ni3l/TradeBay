color pink : fb0066
color gray (text) : 4c4c4c
color green-like : 3adbb9




difficulties: 

auth: /createaccount should send you to /login to enter application


 <div className='flex'>
<span className='text-gray-900 font-bold text-sm'>Condition:</span>{" "}
&nbsp;{" "}
<span
  className={`text-sm ${
    article.condition === "new" && "text-green-500"
  } ${article.condition === "acceptable" && "text-red-500"} ${
    article.condition === "used" && "text-red-500"
  }`}>
  {article.category}
</span>
</div>

Things to do: 
Rating section stars(number of user that rated)


Cart array of object, we use email inside cart object to verify if it's that users cart with articleId

Userwithcart : getAllCart then if currentUser matches a cart email dont create new cart

then addtocart user with cart update cart with new article id 


get cartarticle uses articleId inside cart to fetch article that matches cart articleId 